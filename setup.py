import setuptools

setuptools.setup(
  name                 = "faster_than_requests",
  version              = "9.9.9",
  package_data         = {'': ['*.nim'], '': ['*.cfg']},
  packages=['faster_than_requests'],
)
